/**
 * Vitest global types so test files get describe, test, expect, beforeEach, vi etc.
 * when using test.globals: true in vitest.config.
 */
/// <reference types="vitest/globals" />
/// <reference types="@testing-library/jest-dom" />
